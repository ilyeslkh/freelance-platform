{% extends 'base.html.twig' %}

{% block title %}Liste des Annonces{% endblock %}

{% block body %}
<div>
    <h1 class="text-2xl font-bold mb-6">Liste des Annonces</h1>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        {% for job in jobs %}
            <div class="bg-white shadow p-4 rounded">
                <h2 class="text-lg font-bold">{{ job.title }}</h2>
                <p class="mt-2 text-gray-600">{{ job.description|slice(0, 100) ~ '...' }}</p>
                <p class="mt-2 font-semibold text-blue-600">{{ job.budget }} €</p>
                <a href="#" class="text-blue-500 mt-4 block">Voir l'annonce</a>
            </div>
        {% endfor %}
    </div>
</div>
{% endblock %}
