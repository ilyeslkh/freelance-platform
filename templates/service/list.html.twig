{% extends 'base.html.twig' %}

{% block title %}Liste des Services{% endblock %}

{% block body %}
<div>
    <h1 class="text-2xl font-bold mb-6">Nos Services</h1>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        {% for service in services %}
            <div class="bg-white shadow p-4 rounded">
                <h2 class="text-lg font-bold">{{ service.title }}</h2>
                <p class="mt-2 text-gray-600">{{ service.description|slice(0, 100) ~ '...' }}</p>
                <a href="#" class="text-blue-500 mt-4 block">Voir Détail</a>
                <form method="post" action="{{ path('service_delete', {'id': service.id}) }}">
    <button class="bg-red-500 text-white px-4 py-2 rounded" onclick="return confirm('Voulez-vous vraiment supprimer ce service ?')">Supprimer</button>
</form>

            </div>
        {% endfor %}
    </div>
</div>
{% endblock %}
